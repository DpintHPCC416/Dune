# DUNE
## About this Repo

- `simulation`: contains the codes implemented on CPU platform. We evaluate the performance of origin column sketch, bitmap sketch, scatter sketch on RMT platform, scatter sketch on soft-switch.
- `tofino`: contains the codes implemented on RMT(tofino)